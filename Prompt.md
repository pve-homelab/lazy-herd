I want to create a Herdr.dev plugin written is RUST that is basically a plugin with a bunch of sub-plugins inside. This is because I want to make it really easy to have the trools that I want inside of a sungle plugin that I can remember all the commands for. 

The plugin will be called 'Lazy Herd' and is called that because Most of the sub-plugins allow me to set things up and be lazy from then on in. I basiacally want the plugins to consist of these subplugins that could be called from /lazyherd or something how ever you call plugins in herdr. I have attached a Kanban board that I forked from aanother repo that might be the best option for integrated Kanban board into the plugin (it's also written in rust which is good). Also linked the terminal browser as well but pretty sure its not written in rust and not rerally saure how it works to be honest (i would prefer if everything could run in the tui as I might also use this on Linux servers but if some things dont work then so be it (but try and get everything working if possible). I also want to make sure this plugin is extremnely easy to modify in terms of adding or removing more subplugins to I can update it in the future to add more things that I want. when you call the plugin I want you to get a menu. the menu should look almost like its own pane split down the middle with all of the subplugins on the left that are scrollable with esc being to exit the window. as you scroll through the plugins it gives you a brief description of what the plugin is and if you want to go into the subplugin then you press enter and you are greated with that plugins menu (not sure on hwo to navigate back or if after you have selected one if you exit it goea back to normal screen and you need to do the whole thing again). I have given a few sort of button type direction to help you plan user wokflow of using the plugin windows to get to using the pplugin so take that into condieration. i have also put some other plugins in the same folder that youy can either reference or just delete if not needed. the docs and the marketplkacve are both available on the website if you need context or knowledge on how to build or how other have built things https://herdr.dev/. Make sure that the plugin window that pops up has another option to hover over that alolows you to read through a small documentation guide listing the commands and things so you can read what and how to use the plugin/s. I want you to develop a whole development plan end-to-end without my input and i want to trya fully functioning plugin at the end.





|Lazy Git|Allow users to be logged into a git provided GitHub/GitLab/forgejo/gita (local or cloud) and be able to see a list of the repos in that account, be able to pull and push to that repo once selected and loaded and be able to commit to branches etc (full git functionality)|create / edit / delete (prompts you type name in before delting to confirm)|
|-|-|-|
|<br />Lazy Workspace|Allows you to set up premade workspaces. for example a coding workspace could be 5 preconfigured agents with different jobs and maybe a Kanban setup as well. Another workspace could specifically be a research assistant and be set up for research with the terminal browser|create / edit / delete (prompts you type name in before delting to confirm)|
|Lazy Secrets|Minimal secrets manager for agents to use. Can house things like PAT tokens or other minimal secrets for use with agents|create / edit / delete (prompts you type name in before delting to confirm)|
|Lazy Connect|List of quick SSH profiles to select from for agents to SSH to machines etc|create / edit / delete (prompts you type name in before delting to confirm)|
|Lazy Agents|Create and customise custom agents with master prompts and other things that would be good for pre-made agents|create / edit / delete (prompts you type name in before delting to confirm)|
|Lazy Bootstrap|This is the bootstrap manager for the Lazy Herd. It takes full backups of all of the settings and configurations from every single feature/sub-plugin in the Lazy Herd Plugin. Can import/export all configurations. Maybe to share with someone else, or to put on another instance so you can have the same environment across different machines etc|This would be a multi window panel with the ability to \[Export\|Import\|Bootstrap]<br />-Export: Can do full export of everything, or fine grained export allowing you to choose what sub-plugin to backup and further what setting inside, like you can choose which secrets or ssh profiles you want to export or not export <br />- Import: Allows you to import a file and then edit it, maybe you want to double check and make sure you removed all ssh keys for example, and then you can save it (it will just sit there waiting for bootstrap)<br />- Bootstrap allows you to bootstrap the current Imported config, allowing you to get working in herdr as fast as possible with everything configured exactly as you want to.<br />|
|Lazy Search|has the interminal browser to be able to look things up or acces maybe Grafana dashboards or something what ever it is|have some sort of configuration for this to be able to configure it or fix if it breaks - i haven't looked too much into this so not sure how it works really. |
|Lazy Doctor|Herdr health check + agent health check, basically a big environment health check to make sure everything is working||
|Lazy Config|In depth config menu for configuring Herdr||





